import { Idatastate } from "../FormPage"

interface IpropsInterface {
    children: Idatastate;
  }

const Car: React.FC<IpropsInterface> = ({children}) => {

    return (
        <div>
            
        </div>
    );
}

export default Car;